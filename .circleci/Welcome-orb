# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.3
# Use a package of configuration called an orb.
orbs:
  # Declare a dependency on the welcome-orb
  welcome: circleci/welcome-orb@0.4.1
# Orchestrate or schedule a set of jobs
workflows:
  # Name the workflow "welcome"
  welcome:
    # Run the welcome/run job in its own container
    jobs:
      - welcome/run

      #test flow pass gmright.app
      #look /dor is close and i want to pass in  can you open your way
      #i wish i h-2 had enoght to tell (s-1 that we are working on something now 
      # check )> writer this tsand doc is  indx is mis so code automatical 
      # mywill will not goes away and this is what i wanstand for {<g-gmright }
      # ingrore chech- (echo on) 
      # sound neet not the waut  ahdhb
